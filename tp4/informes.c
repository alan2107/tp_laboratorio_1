#include "informes.h"

int ll_count(LinkedList* this, int (*fn)(void* element)){
	int retorno = -1;
	LinkedList* auxFn = NULL;

	if(this != NULL && fn != NULL){
		for(int i = 0; i < 0; i++){

		}
	}

	return retorno;
}


LinkedList* ll_filter(LinkedList* this, int (*fn)(void* element)){
	int retorno = -1;



	return retorno;
}

LinkedList* ll_map(LinkedList* this, void (*fn)(void* element)){

	int retorno = -1;


return retorno;
}
